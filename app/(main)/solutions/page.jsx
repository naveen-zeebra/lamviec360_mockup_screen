import SolutionsClient from "./SolutionsClient";

export const metadata = {
  title: "Hiring Solutions for Employers | LàmViệc360",
  description: "Job posting, candidate search, applicant tracking, shortlisting, interviews and hiring analytics.",
};

export default function Page() {
  return <SolutionsClient />;
}
