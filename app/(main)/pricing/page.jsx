import PricingClient from "./PricingClient";

export const metadata = {
  title: "Employer Pricing | LàmViệc360",
  description: "Free for job seekers. Employer plans priced in VND for teams of every size.",
};

export default function Page() {
  return <PricingClient />;
}
