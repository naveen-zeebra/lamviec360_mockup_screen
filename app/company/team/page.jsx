import TeamClient from "./TeamClient";

export const metadata = {
  title: "Team Management | Company Workspace",
  description: "Invite team members, assign roles and manage access.",
};

export default function Page() {
  return <TeamClient />;
}
