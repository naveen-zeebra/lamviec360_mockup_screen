import SavedJobsClient from "./SavedJobsClient";

export const metadata = {
  title: "Saved Jobs | LàmViệc360",
  description: "Jobs you've saved to review or apply to later.",
};

export default function Page() {
  return <SavedJobsClient />;
}
