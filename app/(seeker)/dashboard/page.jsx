import DashboardClient from "./DashboardClient";

export const metadata = {
  title: "Dashboard | LàmViệc360",
  description: "Your job search dashboard — recommendations, applications, interviews and notifications.",
};

export default function Page() {
  return <DashboardClient />;
}
