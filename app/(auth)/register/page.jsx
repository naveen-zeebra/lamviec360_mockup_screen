import RegisterClient from "./RegisterClient";

export const metadata = {
  title: "Create Your Job Seeker Account | LàmViệc360",
  description: "Create a free LàmViệc360 job seeker profile and start applying to jobs across Vietnam.",
};

export default function Page() {
  return <RegisterClient />;
}
