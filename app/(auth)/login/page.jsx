import LoginClient from "./LoginClient";

export const metadata = {
  title: "Log in or Sign up | LàmViệc360",
  description: "Log in as a job seeker or company, or create a free LàmViệc360 account.",
};

export default function Page() {
  return <LoginClient />;
}
